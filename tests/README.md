Automated tests are not yet wired into the package scripts.

Current verification completed in this workspace:

- `npm run build`

Recommended next step:

- Add Vitest coverage around `lib/portfolio.js` and the broker CSV row-mapping logic in `components/upload/BrokerCSVUploader.jsx`.
